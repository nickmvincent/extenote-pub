# Shared References Metadata Checker

Quick local web app to review frontmatter metadata and open links.

Reads entries from `../content/shared-references` relative to this folder.

## Run

```bash
python3 server.py
```

Then open http://127.0.0.1:8000 in your browser.
